# /etc/zabbix/zabbix_agent2.d/userparameters.d/swarm.conf
# Requires: docker CLI, curl, zabbix user in the "docker" group (read access to /var/run/docker.sock),
# and Include=/etc/zabbix/zabbix_agent2.d/userparameters.d/*.conf in zabbix_agent2.conf.
UserParameter=swarm.services.get,./swarm.sh
